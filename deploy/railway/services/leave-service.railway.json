{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile",
    "watchPatterns": [
      "leave-service/**",
      "nexusHR-common/**",
      "pom.xml"
    ]
  },
  "deploy": {
    "healthcheckPath": "/api/v1/leaves/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE"
  }
}
