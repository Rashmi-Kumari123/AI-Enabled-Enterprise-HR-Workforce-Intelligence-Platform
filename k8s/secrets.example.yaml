# Copy to secrets.yaml and fill before applying. Never commit real secrets.
apiVersion: v1
kind: Secret
metadata:
  name: nexushr-secrets
  namespace: nexushr
type: Opaque
stringData:
  DB_PASSWORD: admin123
  GRAFANA_PASSWORD: admin
