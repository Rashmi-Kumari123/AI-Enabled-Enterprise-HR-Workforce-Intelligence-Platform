{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile",
    "watchPatterns": [
      "performance-service/**",
      "nexusHR-common/**",
      "pom.xml"
    ]
  },
  "deploy": {
    "healthcheckPath": "/api/v1/performance/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE"
  }
}
